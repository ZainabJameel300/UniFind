import 'package:flutter/material.dart';

class KeepersPage extends StatelessWidget {
  const KeepersPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const SafeArea(
      child: Center(child: Text('keepers')),
    );
  }
}